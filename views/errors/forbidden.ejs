<%- include('../partials/header', { title: 'Forbidden' }) %>
<main class="mx-auto max-w-2xl px-4 py-16 text-center">
  <h1 class="text-2xl font-semibold text-gray-900">403 — Not your role</h1>
  <p class="mt-2 text-gray-600">
    This page requires role <code class="rounded bg-gray-100 px-1 py-0.5 text-sm"><%= required %></code>.
    Your current roles: <code class="rounded bg-gray-100 px-1 py-0.5 text-sm"><%= (have || []).join(', ') || 'none' %></code>.
  </p>
  <a href="/" class="mt-6 inline-block rounded bg-blue-600 px-4 py-2 text-sm font-medium text-white hover:bg-blue-700">
    Back home
  </a>
</main>
<%- include('../partials/footer') %>
